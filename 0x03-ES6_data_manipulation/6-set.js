/**
 *  function, which returns a Set from an array
 *  takes an array of any type of element as its argument
 */
export default function setFromArray(array) {
  const set = new Set(array);
  return set;
}
